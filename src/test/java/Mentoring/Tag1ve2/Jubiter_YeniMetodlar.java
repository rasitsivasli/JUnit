package Mentoring.Tag1ve2;

public class Jubiter_YeniMetodlar {
    /*
@Disabled: Bu annotasyon, bir test metodu veya test sınıfının geçici olarak devre dışı bırakılmasını sağlar.
Bu, bir testi geçici olarak atlamak istediğinizde veya geçici olarak kullanılamayan bir test metodunu belirtmek istediğinizde kullanışlıdır.

@RepeatedTest: Bu annotasyon, bir test metodu belirli bir sayıda tekrarlanacak şekilde işaretlenmesini sağlar.
Bir testi birkaç kez çalıştırmak istediğinizde bu annotasyonu kullanabilirsiniz.

@ParameterizedTest: Bu annotasyon, bir test metodunun birden çok parametre setiyle tekrarlanmasını sağlar.
Farklı parametrelerle aynı testi birkaç kez çalıştırmak istediğinizde bu annotasyonu kullanabilirsiniz.

@TestFactory: Bu annotasyon, testlerin dinamik olarak oluşturulmasını sağlar.
Farklı test durumlarına veya senaryolarına dayalı olarak testlerin oluşturulmasını istediğinizde bu annotasyonu kullanabilirsiniz.

@Nested: Bu annotasyon, iç içe geçmiş test sınıflarını tanımlamak için kullanılır.
Bir test sınıfı içinde başka test sınıfları oluşturarak testlerinizi daha düzenli bir şekilde organize etmek istediğinizde bu annotasyonu kullanabilirsiniz.



@Test: Bu annotasyon, bir test metodu olduğunu belirtmek için kullanılır.
Herhangi bir metodu test metodu olarak işaretlemek için bu annotasyonu kullanabilirsiniz.

@BeforeEach: Bu annotasyon, bir test metodu başlamadan önce çalıştırılacak bir metod belirtmek için kullanılır.
Her test metodu başlamadan önce çalıştırılacak ortak hazırlık işlemlerini bu metod içine yerleştirebilirsiniz.

@AfterEach: Bu annotasyon, bir test metodu tamamlandıktan sonra çalıştırılacak bir metod belirtmek için kullanılır.
Her test metodu tamamlandıktan sonra yapılacak temizlik işlemlerini bu metod içine yerleştirebilirsiniz.

@BeforeAll: Bu annotasyon, test sınıfındaki tüm test metodları başlamadan önce yalnızca bir kez çalıştırılacak bir metodu belirtmek için kullanılır.
Genellikle test sınıfının genel başlangıç işlemleri bu metoda yerleştirilir.

@AfterAll: Bu annotasyon, test sınıfındaki tüm test metodları tamamlandıktan sonra yalnızca bir kez çalıştırılacak bir metodu belirtmek için kullanılır.
Genellikle test sınıfının genel temizlik işlemleri bu metoda yerleştirilir.

@DisplayName: Bu annotasyon, test metodu veya test sınıfının görüntülenmesi için bir isim sağlar.
Bu, testlerin raporlanması veya diğer görsel araçlarla kullanılması için yararlıdır.

     */
}
